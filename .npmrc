# Prevent node-gyp native compilation failures on Windows and non-LTS Node versions (e.g. Node 25)
# Prebuilt binaries for better-sqlite3 and all native modules are bundled in their respective packages
ignore-scripts=true
